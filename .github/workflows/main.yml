name: Auto-assign and notify on new issues

on:
  issues:
    types: [opened]

jobs:
  assign-and-notify:
    runs-on: ubuntu-latest
    permissions:
      issues: write

    steps:
      - name: Assign issue to teamplannerhackathon
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addAssignees({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              assignees: ['teamplannerhackathon']
            });

      - name: Add notification comment
        uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue;
            const comment = `👋 @teamplannerhackathon - A new issue has been automatically assigned to you!

            **Issue:** #${issue.number}
            **Title:** ${issue.title}
            **Author:** @${issue.user.login}
            **Created:** ${new Date(issue.created_at).toLocaleString()}

            Please review and triage this issue.`;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });

      - name: Add triage label
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: ['triage']
            });
